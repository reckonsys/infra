{% extends "nginx_base.conf" %}
{% block content %}
    root %(var_static_app)s;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
        {% include 'nginx_cors.conf' %}
    }
{% endblock %}
