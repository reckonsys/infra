{% extends "nginx_base.conf" %}
{% block content %}
    root %(var_static_app)s;
    index index.html;

    location / {
        {% include 'nginx_cors.conf' %}
        try_files $uri $uri/ /index.html;
    }
{% endblock %}
