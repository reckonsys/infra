{% extends "nginx_base.conf" %}
{% block content %}
    root %(var_static_app)s;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
        {% if nginx_cors %}
        {% include 'nginx_cors.conf' %}
        {% endif %}
    }
{% endblock %}
